import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  image: {
    borderRadius: '50%',
    display: 'block',
    height: '230px',
    margin: '0 auto 8px',
    maxWidth: '230px',
  },
}));
